function color(id) {
    const texto = document.getElementById(id);
    texto.style.color = "red"
};
function color2(id) {
    const texto = document.getElementById(id);
    texto.style.color = "white"
};
